{
  "name": "memeagent-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MemeAgent backend proxy + alpha service (Fastify)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.1.1",
    "fastify": "^5.1.0"
  }
}
